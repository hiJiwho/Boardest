# Dispatch: Reviewer 1 — Milestone 1 (Shared Packages)

## Mission
Independently review all changes in Milestone 1 (`packages/common/**`, `packages/plugins/**`).
Examine:
- Correctness, completeness, robustness, interface conformance.
- Verify `AppSettings.schoolId` serialization, `PlatformCapability` web safety, `TotpService` RFC 6238 implementation, `AnnotationStroke` & `BstPenData` point compatibility, `bst_tbp` dependency resolution and leak disposal.
- Run `flutter test` and `flutter analyze` on affected packages.

## Required Outputs
Write review report in `.agents/reviewer_m1_1/review.md` and standard `handoff.md` with explicit APPROVE or REQUEST_CHANGES verdict.
Report back when finished.
