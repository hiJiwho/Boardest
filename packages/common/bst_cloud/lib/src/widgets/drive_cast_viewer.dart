import 'package:flutter/material.dart';

class DriveCastViewer extends StatelessWidget {
  const DriveCastViewer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('DriveCast Viewer'),
      ),
    );
  }
}
